#!/usr/bin/env node
// Thin forwarder to the development entrypoint used by package.json bin mapping.
// This ensures `./bin/speckit` behaves the same as the published CLI during local dev.
import('./speckit.js');
